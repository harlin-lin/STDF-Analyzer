<UserControl x:Class="UI_Data.Views.DataRaw"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"         
             xmlns:FastWpfGrid="clr-namespace:FastWpfGrid;assembly=FastWpfGrid"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="10"/>
            <ColumnDefinition Width="{Binding SplitterWidth, Mode=OneWay}"/>
            <ColumnDefinition Width="{Binding ChartViewWidth, Mode=OneWay}"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Width="18" Height="18" Margin="0,0,0,0" Command="{Binding ExportToExcel, Mode=OneWay}" />
            </StackPanel>
            <FastWpfGrid:FastGridControl x:Name="grid" Grid.Row="1" Model="{Binding DataTable}">
            </FastWpfGrid:FastGridControl>
        </Grid>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>
        <ContentControl Grid.Column="2" prism:RegionManager.RegionName="{Binding RegionName}"/>
    </Grid>
</UserControl>
