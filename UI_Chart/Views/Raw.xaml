<UserControl x:Class="UI_Chart.Views.Raw"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
             mc:Ignorable="d"         
             d:DesignHeight="700" d:DesignWidth="600"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="{Binding PageCnt}" Width="40" Margin="10,0,10,0"/>
            <Button Width="23" Height="23" Margin="10,0,10,0" Command="{Binding JumpFirstPage, Mode=OneWay}" ToolTip="Save Trend Chart" BorderThickness="0">
                <Button.Background>
                    <ImageBrush ImageSource="/SillyMonkey.Core;component/Resource/icon/leftend.png"/>
                </Button.Background>
            </Button>
            <Button Width="23" Height="23" Margin="10,0,10,0" Command="{Binding JumpPreviousPage, Mode=OneWay}" ToolTip="Save Trend Chart" BorderThickness="0">
                <Button.Background>
                    <ImageBrush ImageSource="/SillyMonkey.Core;component/Resource/icon/left.png"/>
                </Button.Background>
            </Button>
            <Button Width="23" Height="23" Margin="10,0,10,0" Command="{Binding JumpNextPage, Mode=OneWay}" ToolTip="Save Trend Chart" BorderThickness="0">
                <Button.Background>
                    <ImageBrush ImageSource="/SillyMonkey.Core;component/Resource/icon/right.png"/>
                </Button.Background>
            </Button>
            <Button Width="23" Height="23" Margin="10,0,10,0" Command="{Binding JumpLastPage, Mode=OneWay}" ToolTip="Save Trend Chart" BorderThickness="0">
                <Button.Background>
                    <ImageBrush ImageSource="/SillyMonkey.Core;component/Resource/icon/rightend.png"/>
                </Button.Background>
            </Button>
            <TextBox Text="{Binding JumpPage}" Margin="10,0,10,0" Width="40"/>
            <Button Width="23" Height="23" Margin="10,0,10,0" Command="{Binding JumpSelectedPage, Mode=OneWay}" ToolTip="Save Trend Chart" BorderThickness="0">
                <Button.Background>
                    <ImageBrush ImageSource="/SillyMonkey.Core;component/Resource/icon/check.png"/>
                </Button.Background>
            </Button>

        </StackPanel>
        <DataGrid Grid.Row="1" FrozenColumnCount="2"  ItemsSource="{Binding TestItems}" IsReadOnly="True" ClipboardCopyMode="IncludeHeader"/>
    </Grid>
</UserControl>
