<UserControl x:Class="UI_Chart.Views.Trend"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:s="http://schemas.abtsoftware.co.uk/scichart"                
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<oxy:Plot Title="Trend" Grid.Row="0">
            --><!--<oxy:Plot.Axes>
                <oxy:LinearAxis Title="Idx" Position="Bottom" />
                <oxy:LinearAxis Title="Val" Position="Left"/>
            </oxy:Plot.Axes>--><!--
            <oxy:Plot.Series>
                <oxy:LineSeries ItemsSource="{Binding TrendData}"
                                DataFieldX="X" 
                                DataFieldY="Y"
                                StrokeThickness="2"
                                MarkerSize="0"
                                LineStyle="Solid"
                                Color="{Binding LineColor}"
                                MarkerType="None" 
                                />
            </oxy:Plot.Series>
        </oxy:Plot>-->
        <s:SciChartSurface x:Name="sciChart"
                           Grid.Row="0">

            <s:SciChartSurface.RenderableSeries>
                <s:FastLineRenderableSeries DataSeries="{Binding TrendData}" Stroke="DarkOrange" />
            </s:SciChartSurface.RenderableSeries>

            <s:SciChartSurface.XAxis>
                <s:NumericAxis AutoRange="Always" />
            </s:SciChartSurface.XAxis>

            <s:SciChartSurface.YAxis>
                <s:NumericAxis AutoRange="Always" />
            </s:SciChartSurface.YAxis>

            <s:SciChartSurface.ChartModifier>
                <s:ModifierGroup>
                    <s:RubberBandXyZoomModifier IsAnimated="True" />
                    <s:CursorModifier ShowTooltip="False"/>
                    <s:ZoomPanModifier ClipModeX="None"
                                       ExecuteOn="MouseLeftButton"
                                       ReceiveHandledEvents="True" />
                    <s:ZoomExtentsModifier IsAnimated="True" />
                </s:ModifierGroup>
            </s:SciChartSurface.ChartModifier>

        </s:SciChartSurface>

        <GridSplitter Grid.Row="1"/>
        
    </Grid>
</UserControl>
