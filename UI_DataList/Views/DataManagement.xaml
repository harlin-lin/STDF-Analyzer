<UserControl x:Class="UI_DataList.Views.DataManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"     
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol" 
             xmlns:UI_DataList="clr-namespace:UI_DataList"
             xmlns:local="clr-namespace:UI_DataList.ViewModels"
             Name="FileControl"
             d:DesignHeight="300" d:DesignWidth="300"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <UI_DataList:ImgVisibilityCtr x:Key="imgbool"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TreeView x:Name="dataList" Grid.Row="0" ItemsSource="{Binding Files}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectedItemChanged" >
                    <prism:InvokeCommandAction Command="{Binding SelectData}" CommandParameter="{Binding ElementName=dataList,Path=SelectedItem}"/>
                </i:EventTrigger>
                <i:EventTrigger EventName="MouseDoubleClick" >
                    <prism:InvokeCommandAction Command="{Binding OpenData}" CommandParameter="{Binding ElementName=dataList,Path=SelectedItem}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding SubDataList}">
                    <StackPanel Orientation="Horizontal" ToolTip="{Binding NodeTip}">
                        <Image  Source="load.png" Width="12" Height="12"  Visibility="{Binding ExtractedDone, Converter={StaticResource imgbool}}"/>
                        <TextBlock Text="{Binding NodeName}"/>
                        <StackPanel.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Close File" Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext.CmdCloseFile}" CommandParameter="{Binding}"/>
                            </ContextMenu>
                        </StackPanel.ContextMenu>
                    </StackPanel>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type local:FilterNode}" ItemsSource="{Binding SiteList}">
                            <StackPanel ToolTip="{Binding FilePath}">
                                <TextBlock Text="{Binding NodeName}"/>
                                <StackPanel.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Close Filtered Data" Command="{Binding CmdCloseData}" CommandParameter="{Binding}"/>
                                    </ContextMenu>
                                </StackPanel.ContextMenu>
                            </StackPanel>
                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel ToolTip="{Binding FilePath}">
                                        <TextBlock Text="{Binding NodeName}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>
                        </HierarchicalDataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        <GridSplitter Grid.Row="1" VerticalAlignment="Stretch"/>
        <TabControl Grid.Row="2" >
            <TabItem Header="数据汇总">
                <ContentControl prism:RegionManager.RegionName="Region_Summary" />
            </TabItem>
            <TabItem Header="过滤器">
                <ContentControl prism:RegionManager.RegionName="Region_Filter" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
